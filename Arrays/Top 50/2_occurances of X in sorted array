//https://www.codingninjas.com/codestudio/problems/occurrence-of-x-in-a-sorted-array_630456?leftPanelTab=2

*** BRUTE FORCE***
int countOccurences(int arr[], int n, int x)
{
    // Initialize a count variable
    int count = 0;

    // Traverse in the array
    for (int i = 0; i < n; ++i)
    {
        // If current element is x, increment count
        if (arr[i] == x)
        {
            ++count;
        }
    }

    return count;
}

Time Complexity
O(N), Where N is the total number of elements in the array of the size of the array.

 

You visit every element once, hence you make a total of N operations. therefore the time complexity is O(N).

Space Complexity
O(1)

************
