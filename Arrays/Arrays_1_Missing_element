
    int MissingNumber(vector<int>& array, int n) 
    {
        int sum = (n+1)*(n+2) / 2;
        
        for(int i=0;i<n;i++)
          sum = sum - array[i];
        
        return sum;  
    }
    
    //another approach is using xor
